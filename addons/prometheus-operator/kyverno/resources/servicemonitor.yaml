apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: kyverno
spec:
  endpoints:
  - path: /metrics
    targetPort: metrics
    honorLabels: false
    interval: 60s
    scrapeTimeout: 30s
  selector: {}
