apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: kube-green
spec:
  endpoints:
  - path: /metrics
    port: metrics
    honorLabels: false
    interval: 60s
    scrapeTimeout: 30s
    metricRelabelings:
    - action: keep
      regex: controller_runtime.*
      sourceLabels:
      - __name__
  selector:
    matchLabels: {}
