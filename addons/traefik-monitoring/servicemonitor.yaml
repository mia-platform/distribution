sapiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: traefik
  labels: {}
  namespace: NAMESPACE_NAME # Substituted with the one from the deployment
spec:
  endpoints:
  - path: /metrics
    port: traefik
  selector:
    matchLabels: {}
