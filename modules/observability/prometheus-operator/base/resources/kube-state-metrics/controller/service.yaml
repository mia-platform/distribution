apiVersion: v1
kind: Service
metadata:
  name: kube-state-metrics
spec:
  clusterIP: None
  ports:
  - name: web
    port: 8080
    targetPort: web
  - name: metrics
    port: 8081
    targetPort: metrics
